<?php
/**
 * 
 * View -- of Academic/ResultController/resultAction
 * 
 */
	$this->headTitle($this->title);
	
?>
<?php echo $this->tabs_helper(array(2819)); ?>
<div class="card">
	<div class="card-header">
		<div class="btn-group pull-right">
			<?php echo $this->button(2);?>
		</div>
	</div>
	<div class="table-responsive">
	<table id="datatable" class="d-style w-100 table text-dark-m1 text-95 border-y-1 brc-black-tp11 collapsed dtr-table">
                  <!-- add `collapsed` by default ... it will be removed by default -->
                  <!-- thead with .sticky-nav -->
         <thead class="sticky-nav text-primary-d1 text-uppercase text-100">
				<tr>
					<th>#</th>
					<th>Student Name</th>
					<th>Academic year</th>
					<th>Batch</th>
					<th>Organization</th>
					<th>Class</th>
				</tr>
			</thead>
			<tbody>
				<?php
					$i = 1; 
					foreach ($paginator as $row):?>
						<tr data-href="<?php echo $this->button(4,$row['monk_id']); ?>">
							<td><?php echo $i++; ?> </td> 
							<td><?php echo $this->stdname->getColumn($row['monk_id'],'full_name'); ?></td>
							<td><?php echo $row['academic_year']; ?></td>
							<td><?php echo $row['batch']; ?></td>
							<td><?php echo $this->organization->getColumn($row['organization'],'location'); ?></td>
							<td><?php echo $this->classes->getColumn($row['class'],'standard'); ?></td>         
						</tr>
				<?php endforeach;?>
			</tbody>
		</table>
	</div>
	<div class="card-footer d-flex align-items-center">
		<?php echo $this->paginationControl($this->paginator,'Sliding','adm_pagination.phtml', 
			array('route' => 'curriculum/paginator','action'=>'viewstudentlist'));?>
	</div>
</div>
<!--for modal -->
<div id="modal" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="false">
    <div class="modal-dialog">
        <div class="modal-content"> </div>
    </div>
</div>
<script>
$('document').ready(function(){
	$("tr[data-href]").each(function(){ 
		if($(this).attr('data-href')!='#'){
			$(this).css('cursor','pointer');
			$(this).bind('click',function() {
				window.document.location = $(this).attr("data-href");
			});
		}
	});
	$('#dataTables').DataTable();
});
</script>
